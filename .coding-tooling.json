{
  "schemaVersion": 1,
  "profile": "repository-foundation-v1",
  "tiers": {
    "fast": ["format:check", "lint", "build", "test"],
    "full": ["format:check", "lint", "build", "test"]
  },
  "requiredCapabilities": ["format:check", "lint", "build", "test"]
}
